---
import { apiBase } from '../lib/api';
import { readCartSessionId } from '../lib/cart-session';

const sessionId = readCartSessionId(Astro.request);
let cartItemCount = 0;

if (sessionId) {
  try {
    const res = await fetch(`${apiBase}/commerce/carts/${encodeURIComponent(sessionId)}`, {
      headers: { Accept: 'application/json' },
      signal: AbortSignal.timeout(1500), // Quick, safe fallback timeout
    });
    if (res.ok) {
      const json = await res.json();
      if (json.success && json.data && json.data.items) {
        cartItemCount = json.data.items.reduce((acc: number, item: any) => acc + item.quantity, 0);
      }
    }
  } catch (err) {
    // Graceful error fail-safe, leaves count at 0 without blocking render
    cartItemCount = 0;
  }
}

// Binds active search query term from URL safely
const q = Astro.url.searchParams.get('q') || '';
const currentPath = Astro.url.pathname;

// Clean active state evaluation logic
const isShopAllActive = currentPath === '/shop' && !q;
const isPowerActive = currentPath === '/shop' && q === 'charger';
const isSoundActive = currentPath === '/shop' && q === 'earbuds';
const isStorageActive = currentPath === '/shop' && q === 'flash';
const isCarActive = currentPath === '/shop' && q === 'mount';
const isPCActive = false; // PC fallback is neutral to avoid overlap with Shop All active state
---

<header class="bg-white text-gray-900 border-b border-gray-100 sticky top-0 z-50 shadow-sm selection:bg-brand-primary selection:text-gray-900 transition-all duration-300">
  
  <!-- WIDE DESKTOP LAYOUT (min-width: 1024px) -->
  <!-- Single-Row Elegant Architecture: Logo | Unified Segmented Dock | Separator | Search | Separator | Account | Separator | Cart -->
  <div class="hidden lg:block py-3.5 px-6 xl:px-10 max-w-[1600px] mx-auto">
    <div class="flex items-center justify-between gap-6 xl:gap-8 h-10">
      
      <!-- Logo + Category Dock Row Wrap -->
      <div class="flex items-center gap-6 xl:gap-8 h-10">
        <!-- Brand Logo with proper breathing room -->
        <div class="flex items-center flex-shrink-0">
          <a href="/" class="flex items-center group outline-none focus-visible:ring-2 focus-visible:ring-brand-primary rounded-lg transition-all duration-300 py-1 px-2 -ml-2">
            <img
              src="/logos/goldplus-logo-header-exact-old-footprint-117x33.svg"
              width="117"
              height="33"
              alt="GoldPlus"
              class="h-[26px] w-auto object-contain transition-transform duration-300 group-hover:scale-[1.02]"
            />
          </a>
        </div>

        <!-- Unified Segmented Category Dock: Shop All / Power / Sound / Storage / Car / PC -->
        <nav class="flex items-center bg-slate-50/80 px-2 py-1 border border-slate-200/50 rounded-full text-xs font-semibold text-gray-650 h-10 shadow-sm" aria-label="Desktop Categories">
          <!-- Shop All Segment -->
          <a
            href="/shop"
            title="Browse all GoldPlus products"
            aria-label="Browse all GoldPlus products"
            class={`group flex items-center gap-2 px-4 py-1 hover:bg-white rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isShopAllActive ? 'bg-white text-gray-950 font-bold shadow-sm border border-slate-100/50' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-[20px] h-[20px] flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
              isShopAllActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M16 10a4 4 0 0 1-8 0" />
                <path d="M3.103 6.034h17.794" />
                <path d="M3.4 5.467a2 2 0 0 0-.4 1.2V20a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6.667a2 2 0 0 0-.4-1.2l-2-2.667A2 2 0 0 0 17 2H7a2 2 0 0 0-1.6.8z" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Shop All</span>
          </a>

          <div class="h-4 w-px bg-slate-200/80 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- Power Segment -->
          <a
            href="/shop?q=charger"
            title="Power devices: chargers, charging cables and power banks"
            aria-label="Power devices: chargers, charging cables and power banks"
            class={`group flex items-center gap-2 px-4 py-1 hover:bg-white rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isPowerActive ? 'bg-white text-gray-950 font-bold shadow-sm border border-slate-100/50' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-[20px] h-[20px] flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
              isPowerActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="m11 7-3 5h4l-3 5" />
                <path d="M14.856 6H16a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2h-2.935" />
                <path d="M22 14v-4" />
                <path d="M5.14 18H4a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h2.936" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Power</span>
          </a>

          <div class="h-4 w-px bg-slate-200/80 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- Sound Segment -->
          <a
            href="/shop?q=earbuds"
            title="Sound devices: earbuds and portable speakers"
            aria-label="Sound devices: earbuds and portable speakers"
            class={`group flex items-center gap-2 px-4 py-1 hover:bg-white rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isSoundActive ? 'bg-white text-gray-950 font-bold shadow-sm border border-slate-100/50' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-[20px] h-[20px] flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
              isSoundActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M3 14h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7a9 9 0 0 1 18 0v7a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Sound</span>
          </a>

          <div class="h-4 w-px bg-slate-200/80 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- Storage Segment -->
          <a
            href="/shop?q=flash"
            title="Storage devices: flash drives, SD cards and external storage"
            aria-label="Storage devices: flash drives, SD cards and external storage"
            class={`group flex items-center gap-2 px-4 py-1 hover:bg-white rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isStorageActive ? 'bg-white text-gray-950 font-bold shadow-sm border border-slate-100/50' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-[20px] h-[20px] flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
              isStorageActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M10 16h.01" />
                <path d="M2.212 11.577a2 2 0 0 0-.212.896V18a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-5.527a2 2 0 0 0-.212-.896L18.55 5.11A2 2 0 0 0 16.76 4H7.24a2 2 0 0 0-1.79 1.11z" />
                <path d="M21.946 12.013H2.054" />
                <path d="M6 16h.01" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Storage</span>
          </a>

          <div class="h-4 w-px bg-slate-200/80 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- Car Segment -->
          <a
            href="/shop?q=mount"
            title="Car accessories: phone mounts and car chargers"
            aria-label="Car accessories: phone mounts and car chargers"
            class={`group flex items-center gap-2 px-4 py-1 hover:bg-white rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isCarActive ? 'bg-white text-gray-950 font-bold shadow-sm border border-slate-100/50' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-[20px] h-[20px] flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
              isCarActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="m21 8-2 2-1.5-3.7A2 2 0 0 0 15.646 5H8.4a2 2 0 0 0-1.903 1.257L5 10 3 8" />
                <path d="M7 14h.01" />
                <path d="M17 14h.01" />
                <rect width="18" height="8" x="3" y="10" rx="2" />
                <path d="M5 18v2" />
                <path d="M19 18v2" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Car</span>
          </a>

          <div class="h-4 w-px bg-slate-200/80 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- PC Segment -->
          <a
            href="/shop"
            title="PC accessories: mice and sound cards"
            aria-label="PC accessories: mice and sound cards"
            class="group flex items-center gap-2 px-4 py-1 hover:bg-white rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 hover:text-gray-950 text-gray-700"
          >
            <div class="w-[20px] h-[20px] flex items-center justify-center text-slate-400 group-hover:text-brand-primary transition-colors duration-300 flex-shrink-0">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <rect x="5" y="2" width="14" height="20" rx="7" />
                <path d="M12 6v4" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">PC</span>
          </a>
        </nav>
      </div>

      <!-- Soft Structural Separator 1 -->
      <div class="h-6 w-px bg-slate-200 flex-shrink-0 self-center" aria-hidden="true"></div>

      <!-- Right Side Search Form with Premium Border Outlines -->
      <form action="/shop" method="GET" class="flex-grow max-w-[420px] xl:max-w-[500px] relative group flex items-center h-10">
        <div class="relative w-full">
          <input
            type="search"
            name="q"
            value={q}
            placeholder="What are you shopping for?"
            aria-label="Product Search"
            class="w-full pl-10 pr-20 py-2 bg-slate-50/70 hover:bg-slate-100/60 focus:bg-white border border-slate-250 rounded-full focus:border-brand-primary focus:ring-4 focus:ring-brand-primary/10 transition-all duration-300 outline-none text-xs placeholder-gray-400 text-gray-800 h-10 shadow-inner"
          />
          <span class="absolute left-3.5 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-brand-primary transition-colors duration-300">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="m21 21-4.34-4.34" />
              <circle cx="11" cy="11" r="8" />
            </svg>
          </span>
          <button type="submit" class="absolute right-2 top-1/2 -translate-y-1/2 px-4 py-1.5 text-[10px] font-black uppercase tracking-widest text-slate-500 hover:text-slate-900 hover:bg-slate-100 transition-all duration-300 outline-none rounded-full">
            Search
          </button>
        </div>
      </form>

      <!-- Soft Structural Separator 2 -->
      <div class="h-6 w-px bg-slate-200 flex-shrink-0 self-center" aria-hidden="true"></div>

      <!-- Account / Cart Actions with Premium Hover/Focus styling -->
      <div class="flex items-center gap-4 flex-shrink-0 h-10">
        <a href="/account" class="text-xs font-bold text-gray-600 hover:text-gray-900 hover:bg-slate-50 px-3 py-2 transition-all duration-300 focus-visible:ring-2 focus-visible:ring-brand-primary outline-none rounded-full">
          Account
        </a>
        
        <!-- Soft Structural Separator 3 -->
        <div class="h-6 w-px bg-slate-200 flex-shrink-0 self-center" aria-hidden="true"></div>

        <!-- Cart Action Capsule Button -->
        <a href="/cart" class="group flex items-center gap-2.5 px-4.5 py-1.5 bg-slate-50 hover:bg-slate-100/90 border border-slate-200/60 rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary h-10 shadow-sm">
          <span class="text-xs font-bold text-gray-700 group-hover:text-gray-950 transition-colors">Cart</span>
          <span class="bg-brand-primary text-gray-950 text-[10px] font-black px-2.5 py-0.5 rounded-full transition-transform duration-300 group-hover:scale-108 shadow-xs">
            {cartItemCount}
          </span>
        </a>
      </div>

    </div>
  </div>

  <!-- MEDIUM / TABLET LAYOUT (min-width: 768px and max-width: 1023px) -->
  <div class="hidden md:block lg:hidden border-b border-gray-100/50">
    <div class="py-2.5 px-6 max-w-7xl mx-auto flex items-center justify-between gap-6 h-12">
      <!-- Brand Logo -->
      <div class="flex items-center flex-shrink-0">
        <a href="/" class="flex items-center group outline-none focus-visible:ring-2 focus-visible:ring-brand-primary rounded-lg transition-all duration-300 py-1">
          <img
            src="/logos/goldplus-logo-header-exact-old-footprint-117x33.svg"
            width="117"
            height="33"
            alt="GoldPlus"
            class="h-[24px] w-auto object-contain"
          />
        </a>
      </div>

      <!-- Integrated Search Bar -->
      <form action="/shop" method="GET" class="flex-grow max-w-md relative group mx-auto flex items-center h-9">
        <div class="relative w-full">
          <input
            type="search"
            name="q"
            value={q}
            placeholder="Search products..."
            aria-label="Product Search"
            class="w-full pl-10 pr-20 py-2 bg-slate-50 border border-slate-200/80 rounded-full focus:bg-white focus:border-brand-primary focus:ring-4 focus:ring-brand-primary/10 transition-all duration-300 outline-none text-xs placeholder-gray-400 text-gray-800 h-9"
          />
          <span class="absolute left-3.5 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-brand-primary transition-colors duration-300">
            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="m21 21-4.34-4.34" />
              <circle cx="11" cy="11" r="8" />
            </svg>
          </span>
          <button type="submit" class="absolute right-2.5 top-1/2 -translate-y-1/2 px-3 py-1 text-[10px] font-black uppercase tracking-widest text-slate-500 hover:text-slate-900 transition-colors duration-300 outline-none rounded-full">
            Search
          </button>
        </div>
      </form>

      <!-- Actions -->
      <div class="flex items-center gap-4 flex-shrink-0 h-9">
        <a href="/account" class="text-xs font-bold text-gray-650 hover:text-gray-950 transition-colors py-1.5 focus-visible:ring-2 focus-visible:ring-brand-primary outline-none rounded-full">
          Account
        </a>
        <div class="h-5 w-px bg-slate-200"></div>
        <a href="/cart" class="group flex items-center gap-2 px-3 py-1 bg-slate-50 hover:bg-slate-100 border border-slate-200/60 rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary h-9 shadow-sm">
          <span class="text-xs font-bold text-gray-700 group-hover:text-gray-950 transition-colors">Cart</span>
          <span class="bg-brand-primary text-gray-950 text-[10px] font-black px-2 py-0.5 rounded-full transition-transform duration-300 group-hover:scale-108">
            {cartItemCount}
          </span>
        </a>
      </div>
    </div>

    <!-- Bottom Row: Centered Category Dock -->
    <div class="bg-slate-50/50 py-1.5 border-t border-slate-100">
      <div class="flex items-center justify-center">
        <nav class="flex items-center bg-white p-0.5 border border-slate-200/50 rounded-full text-xs font-semibold text-gray-750 h-9 shadow-xs" aria-label="Medium Categories">
          <!-- Shop All Segment -->
          <a
            href="/shop"
            title="Browse all GoldPlus products"
            aria-label="Browse all GoldPlus products"
            class={`group flex items-center gap-2 px-3 py-0.5 hover:bg-slate-50 rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isShopAllActive ? 'bg-slate-50 text-gray-950 font-bold shadow-xs' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 ${
              isShopAllActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M16 10a4 4 0 0 1-8 0" />
                <path d="M3.103 6.034h17.794" />
                <path d="M3.4 5.467a2 2 0 0 0-.4 1.2V20a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6.667a2 2 0 0 0-.4-1.2l-2-2.667A2 2 0 0 0 17 2H7a2 2 0 0 0-1.6.8z" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Shop All</span>
          </a>

          <div class="h-3.5 w-px bg-slate-200 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- Power Segment -->
          <a
            href="/shop?q=charger"
            title="Power devices: chargers, charging cables and power banks"
            aria-label="Power devices: chargers, charging cables and power banks"
            class={`group flex items-center gap-2 px-3 py-0.5 hover:bg-slate-50 rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isPowerActive ? 'bg-slate-50 text-gray-950 font-bold shadow-xs' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 ${
              isPowerActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="m11 7-3 5h4l-3 5" />
                <path d="M14.856 6H16a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2h-2.935" />
                <path d="M22 14v-4" />
                <path d="M5.14 18H4a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h2.936" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Power</span>
          </a>

          <div class="h-3.5 w-px bg-slate-200 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- Sound Segment -->
          <a
            href="/shop?q=earbuds"
            title="Sound devices: earbuds and portable speakers"
            aria-label="Sound devices: earbuds and portable speakers"
            class={`group flex items-center gap-2 px-3 py-0.5 hover:bg-slate-50 rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isSoundActive ? 'bg-slate-50 text-gray-950 font-bold shadow-xs' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 ${
              isSoundActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M3 14h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7a9 9 0 0 1 18 0v7a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Sound</span>
          </a>

          <div class="h-3.5 w-px bg-slate-200 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- Storage Segment -->
          <a
            href="/shop?q=flash"
            title="Storage devices: flash drives, SD cards and external storage"
            aria-label="Storage devices: flash drives, SD cards and external storage"
            class={`group flex items-center gap-2 px-3 py-0.5 hover:bg-slate-50 rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isStorageActive ? 'bg-slate-50 text-gray-950 font-bold shadow-xs' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 ${
              isStorageActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M10 16h.01" />
                <path d="M2.212 11.577a2 2 0 0 0-.212.896V18a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-5.527a2 2 0 0 0-.212-.896L18.55 5.11A2 2 0 0 0 16.76 4H7.24a2 2 0 0 0-1.79 1.11z" />
                <path d="M21.946 12.013H2.054" />
                <path d="M6 16h.01" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Storage</span>
          </a>

          <div class="h-3.5 w-px bg-slate-200 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- Car Segment -->
          <a
            href="/shop?q=mount"
            title="Car accessories: phone mounts and car chargers"
            aria-label="Car accessories: phone mounts and car chargers"
            class={`group flex items-center gap-2 px-3 py-0.5 hover:bg-slate-50 rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 ${
              isCarActive ? 'bg-slate-50 text-gray-950 font-bold shadow-xs' : 'hover:text-gray-950 text-gray-700'
            }`}
          >
            <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 ${
              isCarActive 
                ? 'text-brand-primary' 
                : 'text-slate-400 group-hover:text-brand-primary'
            }`}>
              <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="m21 8-2 2-1.5-3.7A2 2 0 0 0 15.646 5H8.4a2 2 0 0 0-1.903 1.257L5 10 3 8" />
                <path d="M7 14h.01" />
                <path d="M17 14h.01" />
                <rect width="18" height="8" x="3" y="10" rx="2" />
                <path d="M5 18v2" />
                <path d="M19 18v2" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">Car</span>
          </a>

          <div class="h-3.5 w-px bg-slate-200 self-center flex-shrink-0" aria-hidden="true"></div>

          <!-- PC Segment -->
          <a
            href="/shop"
            title="PC accessories: mice and sound cards"
            aria-label="PC accessories: mice and sound cards"
            class="group flex items-center gap-2 px-3 py-0.5 hover:bg-slate-50 rounded-full transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-95 h-8 hover:text-gray-950 text-gray-700"
          >
            <div class="w-5 h-5 flex items-center justify-center text-slate-400 group-hover:text-brand-primary transition-colors duration-300">
              <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <rect x="5" y="2" width="14" height="20" rx="7" />
                <path d="M12 6v4" />
              </svg>
            </div>
            <span class="text-[13px] font-semibold transition-colors duration-300">PC</span>
          </a>
        </nav>
      </div>
    </div>
  </div>

  <!-- MOBILE LAYOUT (under 768px) -->
  <div class="md:hidden">
    <div class="flex items-center justify-between px-4 py-3 bg-white h-14 border-b border-slate-50">
      <!-- Brand Logo -->
      <a href="/" class="flex items-center group outline-none focus-visible:ring-2 focus-visible:ring-brand-primary rounded-lg px-2 -ml-2 transition-all duration-300 h-10">
        <img
          src="/logos/goldplus-logo-header-exact-old-footprint-117x33.svg"
          width="117"
          height="33"
          alt="GoldPlus"
          class="h-[22px] w-auto object-contain"
        />
      </a>

      <!-- Right Side Actions: Cart | Menu with at least 44px tap target size -->
      <div class="flex items-center gap-3 h-11">
        <!-- Cart Badge Link -->
        <a href="/cart" aria-label="View shopping cart" class="relative flex items-center justify-center w-11 h-11 bg-slate-50 border border-slate-200/60 rounded-full outline-none focus-visible:ring-2 focus-visible:ring-brand-primary transition-colors">
          <svg class="w-5 h-5 text-gray-700" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
          </svg>
          <span class="absolute -top-0.5 -right-0.5 bg-brand-primary text-gray-950 text-[9px] font-black w-5 h-5 rounded-full flex items-center justify-center shadow-xs">
            {cartItemCount}
          </span>
        </a>

        <!-- Hamburger Drawer Button with at least 44px tap target size (w-11 h-11) -->
        <button
          type="button"
          id="mobile-menu-toggle"
          aria-expanded="false"
          aria-controls="mobile-menu-drawer"
          aria-label="Toggle Navigation Drawer"
          class="flex items-center justify-center w-11 h-11 bg-slate-50 border border-slate-200/60 rounded-full outline-none focus-visible:ring-2 focus-visible:ring-brand-primary transition-all active:scale-95"
        >
          <svg id="hamburger-icon" class="w-5 h-5 text-gray-700" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
          <svg id="close-icon" class="hidden w-5 h-5 text-gray-700" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile Horizontal Category Chips Track -->
    <div class="relative w-full">
      <div class="flex items-center gap-2 overflow-x-auto scroll-smooth pl-4 pr-6 py-2 bg-white border-b border-slate-100 no-scrollbar h-[44px] scroll-pl-4 scroll-pr-6">
        <!-- Shop All Segment -->
        <a href="/shop" class={`flex items-center gap-1.5 pl-1.5 pr-3 py-1 border border-slate-200 rounded-full text-xs font-semibold flex-shrink-0 transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary ${
          isShopAllActive 
            ? 'bg-brand-primary/10 border-brand-primary/40 text-gray-950 font-bold shadow-xs' 
            : 'bg-slate-50/55 text-gray-700 hover:text-gray-950'
        }`}>
          <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
            isShopAllActive ? 'text-brand-primary' : 'text-slate-400'
          }`}>
            <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M16 10a4 4 0 0 1-8 0" />
              <path d="M3.103 6.034h17.794" />
              <path d="M3.4 5.467a2 2 0 0 0-.4 1.2V20a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6.667a2 2 0 0 0-.4-1.2l-2-2.667A2 2 0 0 0 17 2H7a2 2 0 0 0-1.6.8z" />
            </svg>
          </div>
          <span class="text-xs">Shop All</span>
        </a>

        <!-- Power Segment -->
        <a href="/shop?q=charger" class={`flex items-center gap-1.5 pl-1.5 pr-3 py-1 border border-slate-200 rounded-full text-xs font-semibold flex-shrink-0 transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary ${
          isPowerActive 
            ? 'bg-brand-primary/10 border-brand-primary/40 text-gray-950 font-bold shadow-xs' 
            : 'bg-slate-50/55 text-gray-700 hover:text-gray-950'
        }`}>
          <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
            isPowerActive ? 'text-brand-primary' : 'text-slate-400'
          }`}>
            <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <path d="m11 7-3 5h4l-3 5" />
              <path d="M14.856 6H16a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2h-2.935" />
              <path d="M22 14v-4" />
              <path d="M5.14 18H4a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h2.936" />
            </svg>
          </div>
          <span class="text-xs">Power</span>
        </a>

        <!-- Sound Segment -->
        <a href="/shop?q=earbuds" class={`flex items-center gap-1.5 pl-1.5 pr-3 py-1 border border-slate-200 rounded-full text-xs font-semibold flex-shrink-0 transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary ${
          isSoundActive 
            ? 'bg-brand-primary/10 border-brand-primary/40 text-gray-950 font-bold shadow-xs' 
            : 'bg-slate-50/55 text-gray-700 hover:text-gray-950'
        }`}>
          <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
            isSoundActive ? 'text-brand-primary' : 'text-slate-400'
          }`}>
            <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M3 14h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7a9 9 0 0 1 18 0v7a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3" />
            </svg>
          </div>
          <span class="text-xs">Sound</span>
        </a>

        <!-- Storage Segment -->
        <a href="/shop?q=flash" class={`flex items-center gap-1.5 pl-1.5 pr-3 py-1 border border-slate-200 rounded-full text-xs font-semibold flex-shrink-0 transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary ${
          isStorageActive 
            ? 'bg-brand-primary/10 border-brand-primary/40 text-gray-950 font-bold shadow-xs' 
            : 'bg-slate-50/55 text-gray-700 hover:text-gray-950'
        }`}>
          <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
            isStorageActive ? 'text-brand-primary' : 'text-slate-400'
          }`}>
            <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M10 16h.01" />
              <path d="M2.212 11.577a2 2 0 0 0-.212.896V18a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-5.527a2 2 0 0 0-.212-.896L18.55 5.11A2 2 0 0 0 16.76 4H7.24a2 2 0 0 0-1.79 1.11z" />
              <path d="M21.946 12.013H2.054" />
              <path d="M6 16h.01" />
            </svg>
          </div>
          <span class="text-xs">Storage</span>
        </a>

        <!-- Car Segment -->
        <a href="/shop?q=mount" class={`flex items-center gap-1.5 pl-1.5 pr-3 py-1 border border-slate-200 rounded-full text-xs font-semibold flex-shrink-0 transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary ${
          isCarActive 
            ? 'bg-brand-primary/10 border-brand-primary/40 text-gray-950 font-bold shadow-xs' 
            : 'bg-slate-50/55 text-gray-700 hover:text-gray-950'
        }`}>
          <div class={`w-5 h-5 flex items-center justify-center transition-colors duration-300 flex-shrink-0 ${
            isCarActive ? 'text-brand-primary' : 'text-slate-400'
          }`}>
            <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <path d="m21 8-2 2-1.5-3.7A2 2 0 0 0 15.646 5H8.4a2 2 0 0 0-1.903 1.257L5 10 3 8" />
              <path d="M7 14h.01" />
              <path d="M17 14h.01" />
              <rect width="18" height="8" x="3" y="10" rx="2" />
              <path d="M5 18v2" />
              <path d="M19 18v2" />
            </svg>
          </div>
          <span class="text-xs">Car</span>
        </a>

        <!-- PC Segment -->
        <a href="/shop" class="flex items-center gap-1.5 pl-1.5 pr-3 py-1 bg-slate-50/55 border border-slate-200 rounded-full text-gray-700 text-xs font-semibold flex-shrink-0 transition-all duration-300 hover:text-gray-950 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary">
          <div class="w-5 h-5 flex items-center justify-center text-slate-400 flex-shrink-0">
            <svg class="w-4.5 h-4.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <rect x="5" y="2" width="14" height="20" rx="7" />
              <path d="M12 6v4" />
            </svg>
          </div>
          <span class="text-xs">PC</span>
        </a>

        <!-- End spacer -->
        <span aria-hidden="true" class="block w-4 flex-shrink-0"></span>
      </div>
      <!-- Right gradient overlay -->
      <div class="absolute right-0 top-0 bottom-0 w-8 bg-gradient-to-l from-white to-transparent pointer-events-none z-10" aria-hidden="true"></div>
    </div>
  </div>

  <!-- Mobile Dropdown Drawer Panel -->
  <div
    id="mobile-menu-drawer"
    class="hidden md:hidden border-t border-slate-100 bg-white shadow-inner transition-all duration-300"
  >
    <div class="px-5 py-6 space-y-6">
      <!-- Mobile Search Form -->
      <form action="/shop" method="GET" class="relative">
        <input
          type="search"
          name="q"
          value={q}
          placeholder="Search products..."
          aria-label="Mobile Product Search"
          class="w-full pl-10 pr-4 py-3 bg-slate-50 border border-slate-200/80 rounded-full focus:bg-white focus:border-brand-primary focus:ring-4 focus:ring-brand-primary/10 outline-none text-xs font-medium placeholder-gray-400 text-gray-800 transition-all duration-300 shadow-inner"
        />
        <span class="absolute left-3.5 top-1/2 -translate-y-1/2 text-gray-400">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="m21 21-4.34-4.34" />
            <circle cx="11" cy="11" r="8" />
          </svg>
        </span>
      </form>

      <!-- Products Navigation Link -->
      <div>
        <a
          href="/shop"
          title="Browse all GoldPlus products"
          aria-label="Browse all GoldPlus products"
          class="w-full text-center py-4 bg-brand-primary hover:bg-brand-primary/95 text-gray-900 font-bold uppercase tracking-widest rounded-xl text-xs block transition-all duration-300 shadow-md shadow-brand-primary/15"
        >
          Shop All Products
        </a>
      </div>

      <!-- General Navigation Links -->
      <div class="grid grid-cols-1 gap-2.5 pt-4 border-t border-slate-100 text-sm font-bold text-gray-800">
        <a href="/account" class="flex items-center justify-between px-4.5 py-4 bg-slate-50 hover:bg-white hover:border-gray-800 border border-slate-200/70 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary">
          <span>Account Profile</span>
          <span class="text-slate-400">&rarr;</span>
        </a>
        <a href="/cart" class="flex items-center justify-between px-4.5 py-4 bg-slate-50 hover:bg-white hover:border-gray-800 border border-slate-200/70 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary">
          <span>Shopping Cart</span>
          <span class="text-slate-400">&rarr;</span>
        </a>
        <a href="/verification" class="flex items-center justify-between px-4.5 py-4 bg-slate-50 hover:bg-white hover:border-gray-800 border border-slate-200/70 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary">
          <span>Verify Authenticity</span>
          <span class="text-slate-400">&rarr;</span>
        </a>
        <a href="/support" class="flex items-center justify-between px-4.5 py-4 bg-slate-50 hover:bg-white hover:border-gray-800 border border-slate-200/70 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary">
          <span>Support Hub</span>
          <span class="text-slate-400">&rarr;</span>
        </a>
        <a href="/dealers/apply" class="flex items-center justify-between px-4.5 py-4 bg-slate-50 hover:bg-white hover:border-gray-800 border border-slate-200/70 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary">
          <span>Become a Dealer</span>
          <span class="text-slate-400">&rarr;</span>
        </a>
      </div>
    </div>
  </div>
</header>

<style>
  /* Custom scrollbar hiding for horizontal category rail */
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
</style>

<script>
  // Mobile drawer interactions
  const toggleBtn = document.getElementById('mobile-menu-toggle');
  const drawer = document.getElementById('mobile-menu-drawer');
  const hamburgerIcon = document.getElementById('hamburger-icon');
  const closeIcon = document.getElementById('close-icon');

  if (toggleBtn && drawer && hamburgerIcon && closeIcon) {
    toggleBtn.addEventListener('click', () => {
      const isExpanded = toggleBtn.getAttribute('aria-expanded') === 'true';
      toggleBtn.setAttribute('aria-expanded', (!isExpanded).toString());
      
      if (isExpanded) {
        drawer.classList.add('hidden');
        hamburgerIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
      } else {
        drawer.classList.remove('hidden');
        hamburgerIcon.classList.add('hidden');
        closeIcon.classList.remove('hidden');
      }
    });

    // Close mobile drawer on any category/nav click
    drawer.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        toggleBtn.setAttribute('aria-expanded', 'false');
        drawer.classList.add('hidden');
        hamburgerIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
      });
    });

    // Close mobile menu on Escape key press
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && toggleBtn.getAttribute('aria-expanded') === 'true') {
        toggleBtn.setAttribute('aria-expanded', 'false');
        drawer.classList.add('hidden');
        hamburgerIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
        toggleBtn.focus();
      }
    });
  }
</script>
