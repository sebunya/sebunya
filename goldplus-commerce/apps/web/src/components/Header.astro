---
import { apiBase } from '../lib/api';
import { readCartSessionId } from '../lib/cart-session';

const sessionId = readCartSessionId(Astro.request);
let cartItemCount = 0;

if (sessionId) {
  try {
    const res = await fetch(`${apiBase}/commerce/carts/${encodeURIComponent(sessionId)}`, {
      headers: { Accept: 'application/json' },
      signal: AbortSignal.timeout(1500), // Quick, safe fallback timeout
    });
    if (res.ok) {
      const json = await res.json();
      if (json.success && json.data && json.data.items) {
        cartItemCount = json.data.items.reduce((acc: number, item: any) => acc + item.quantity, 0);
      }
    }
  } catch (err) {
    // Graceful error fail-safe, leaves count at 0 without blocking render
    cartItemCount = 0;
  }
}

// Binds active search query term from URL safely
const q = Astro.url.searchParams.get('q') || '';
---

<header class="bg-white text-gray-900 border-b border-gray-100 sticky top-0 z-50 shadow-sm selection:bg-brand-primary selection:text-gray-900">
  
  <!-- WIDE DESKTOP LAYOUT (min-width: 1024px) -->
  <!-- Single-Row Elegant Architecture: GoldPlus | Unified Segmented Dock | Search | Account | Cart -->
  <div class="hidden lg:block py-3 px-4 xl:px-8 max-w-[1600px] mx-auto">
    <div class="flex items-center justify-between gap-4">
      
      <!-- Brand Logo -->
      <div class="flex items-center flex-shrink-0">
        <a href="/" class="flex items-center gap-2 group outline-none focus-visible:ring-2 focus-visible:ring-brand-primary rounded-lg transition-all duration-300">
          <span class="text-xl font-black tracking-tight text-gray-950">
            Gold<span class="text-brand-primary group-hover:text-gray-950 transition-colors duration-300">Plus</span>
          </span>
        </a>
      </div>

      <!-- Unified Segmented Category Dock: Shop All / Power / Sound / Storage / Car / PC -->
      <nav class="flex items-center bg-gray-50/60 p-1 border border-gray-200/80 rounded-2xl text-xs font-semibold text-gray-700" aria-label="Desktop Categories">
        <!-- Shop All Segment (Slate) -->
        <a
          href="/shop"
          title="Browse all GoldPlus products"
          aria-label="Browse all GoldPlus products"
          class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-white hover:text-slate-900 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
        >
          <!-- Icon Tile -->
          <div class="w-8 h-8 rounded-lg bg-slate-50 border border-slate-100 flex items-center justify-center text-slate-500 group-hover:bg-slate-100 group-hover:border-slate-200 group-hover:text-slate-700 transition-colors duration-300 flex-shrink-0">
            <!-- 2x2 grid monoline icon -->
            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <rect x="3" y="3" width="7" height="7" rx="1.5"></rect>
              <rect x="14" y="3" width="7" height="7" rx="1.5"></rect>
              <rect x="3" y="14" width="7" height="7" rx="1.5"></rect>
              <rect x="14" y="14" width="7" height="7" rx="1.5"></rect>
            </svg>
          </div>
          <span class="text-[13px] font-semibold text-gray-700 group-hover:text-slate-900 transition-colors duration-300">Shop All</span>
        </a>

        <!-- Divider line -->
        <div class="h-4 w-px bg-gray-200"></div>

        <!-- Power Segment (Emerald) -->
        <a
          href="/shop?q=charger"
          title="Power devices: chargers, charging cables and power banks"
          aria-label="Power devices: chargers, charging cables and power banks"
          class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-white hover:text-emerald-800 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
        >
          <!-- Icon Tile -->
          <div class="w-8 h-8 rounded-lg bg-emerald-50 border border-emerald-100 flex items-center justify-center text-emerald-600 group-hover:bg-emerald-100/80 group-hover:border-emerald-200 group-hover:text-emerald-700 transition-colors duration-300 flex-shrink-0">
            <!-- Battery with charge bolt monoline icon -->
            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <rect x="2" y="6" width="16" height="12" rx="2"></rect>
              <path d="M22 10v4M10 9l-2 3h4l-2 3"></path>
            </svg>
          </div>
          <span class="text-[13px] font-semibold text-gray-700 group-hover:text-emerald-800 transition-colors duration-300">Power</span>
        </a>

        <!-- Divider line -->
        <div class="h-4 w-px bg-gray-200"></div>

        <!-- Sound Segment (Sky Blue) -->
        <a
          href="/shop?q=earbuds"
          title="Sound devices: earbuds and portable speakers"
          aria-label="Sound devices: earbuds and portable speakers"
          class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-white hover:text-sky-800 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
        >
          <!-- Icon Tile -->
          <div class="w-8 h-8 rounded-lg bg-sky-50 border border-sky-100 flex items-center justify-center text-sky-600 group-hover:bg-sky-100/80 group-hover:border-sky-200 group-hover:text-sky-700 transition-colors duration-300 flex-shrink-0">
            <!-- Modern monoline headphones outline icon -->
            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M3 18v-6c0-4.97 4.03-9 9-9s9 4.03 9 9v6"></path>
              <path d="M21 19a2 2 0 01-2 2h-1a2 2 0 01-2-2v-3a2 2 0 012-2h3v5zM3 19a2 2 0 002 2h1a2 2 0 002-2v-3a2 2 0 00-2-2H3v5z"></path>
            </svg>
          </div>
          <span class="text-[13px] font-semibold text-gray-700 group-hover:text-sky-800 transition-colors duration-300">Sound</span>
        </a>

        <!-- Divider line -->
        <div class="h-4 w-px bg-gray-200"></div>

        <!-- Storage Segment (Amber) -->
        <a
          href="/shop?q=flash"
          title="Storage devices: flash drives, SD cards and external storage"
          aria-label="Storage devices: flash drives, SD cards and external storage"
          class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-white hover:text-amber-850 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
        >
          <!-- Icon Tile -->
          <div class="w-8 h-8 rounded-lg bg-amber-50 border border-amber-100 flex items-center justify-center text-amber-600 group-hover:bg-amber-100/80 group-hover:border-amber-200 group-hover:text-amber-700 transition-colors duration-300 flex-shrink-0">
            <!-- Monoline USB flash drive icon -->
            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <rect x="6" y="8" width="12" height="11" rx="2"></rect>
              <rect x="9" y="3" width="6" height="5"></rect>
              <path d="M11 5h2"></path>
            </svg>
          </div>
          <span class="text-[13px] font-semibold text-gray-700 group-hover:text-amber-850 transition-colors duration-300">Storage</span>
        </a>

        <!-- Divider line -->
        <div class="h-4 w-px bg-gray-200"></div>

        <!-- Car Segment (Teal) -->
        <a
          href="/shop?q=mount"
          title="Car accessories: phone mounts and car chargers"
          aria-label="Car accessories: phone mounts and car chargers"
          class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-white hover:text-teal-850 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
        >
          <!-- Icon Tile -->
          <div class="w-8 h-8 rounded-lg bg-teal-50 border border-teal-100 flex items-center justify-center text-teal-600 group-hover:bg-teal-100/80 group-hover:border-teal-200 group-hover:text-teal-700 transition-colors duration-300 flex-shrink-0">
            <!-- Front profile car monoline icon -->
            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M7 10l1.3-4.1a1 1 0 011-.9h5.4a1 1 0 011 .9L17 10"></path>
              <rect x="3" y="10" width="18" height="6" rx="2.5"></rect>
              <circle cx="6.5" cy="13" r="0.5" fill="currentColor"></circle>
              <circle cx="17.5" cy="13" r="0.5" fill="currentColor"></circle>
              <rect x="5" y="16" width="2" height="3" rx="0.5"></rect>
              <rect x="17" y="16" width="2" height="3" rx="0.5"></rect>
            </svg>
          </div>
          <span class="text-[13px] font-semibold text-gray-700 group-hover:text-teal-850 transition-colors duration-300">Car</span>
        </a>

        <!-- Divider line -->
        <div class="h-4 w-px bg-gray-200"></div>

        <!-- PC Segment (Indigo) -->
        <a
          href="/shop"
          title="PC accessories: mice and sound cards"
          aria-label="PC accessories: mice and sound cards"
          class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-white hover:text-indigo-850 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
        >
          <!-- Icon Tile -->
          <div class="w-8 h-8 rounded-lg bg-indigo-50 border border-indigo-100 flex items-center justify-center text-indigo-600 group-hover:bg-indigo-100/80 group-hover:border-indigo-200 group-hover:text-indigo-700 transition-colors duration-300 flex-shrink-0">
            <!-- Ergonomic computer mouse monoline icon -->
            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
              <rect x="7" y="3" width="10" height="18" rx="5"></rect>
              <path d="M12 6v4M7 11h10"></path>
            </svg>
          </div>
          <span class="text-[13px] font-semibold text-gray-700 group-hover:text-indigo-850 transition-colors duration-300">PC</span>
        </a>
      </nav>

      <!-- Right Side Search Form -->
      <form action="/shop" method="GET" class="flex-grow max-w-[240px] xl:max-w-[320px] relative group flex items-center">
        <div class="relative w-full">
          <input
            type="search"
            name="q"
            value={q}
            placeholder="Search chargers, earbuds, power banks..."
            aria-label="Product Search"
            class="w-full pl-9 pr-20 py-2.5 bg-gray-50 hover:bg-gray-100/60 focus:bg-white border border-gray-200/80 rounded-xl focus:border-brand-primary focus:ring-4 focus:ring-brand-primary/10 transition-all duration-300 outline-none text-xs placeholder-gray-400 text-gray-800"
          />
          <span class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-brand-primary transition-colors duration-300">
            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
          </span>
          <button type="submit" class="absolute right-2 top-1/2 -translate-y-1/2 px-3 py-1 text-[10px] font-black uppercase tracking-widest text-gray-500 hover:text-gray-950 transition-colors duration-300 outline-none rounded-lg">
            Search
          </button>
        </div>
      </form>

      <!-- Account & Cart Actions -->
      <div class="flex items-center gap-4 flex-shrink-0">
        <a href="/account" class="text-xs font-bold text-gray-650 hover:text-gray-950 transition-colors py-1.5 focus-visible:ring-2 focus-visible:ring-brand-primary outline-none rounded-lg">
          Account
        </a>
        <div class="h-3 w-px bg-gray-200"></div>
        <a href="/cart" class="group flex items-center gap-2 px-3 py-1.5 bg-gray-50 hover:bg-gray-100/80 border border-gray-100 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary">
          <span class="text-xs font-bold text-gray-705 group-hover:text-gray-950 transition-colors">Cart</span>
          <span class="bg-brand-primary text-gray-950 text-[10px] font-black px-2 py-0.5 rounded-full transition-transform duration-300 group-hover:scale-110">
            {cartItemCount}
          </span>
        </a>
      </div>

    </div>
  </div>

  <!-- MEDIUM / TABLET LAYOUT (min-width: 768px and max-width: 1023px) -->
  <!-- Two-Row Architecture: Top row (GoldPlus | Search | Account | Cart) + Bottom row (Unified Segmented Dock) -->
  <div class="hidden md:block lg:hidden">
    <!-- Top Row -->
    <div class="py-3 px-6 max-w-7xl mx-auto flex items-center justify-between gap-6">
      
      <!-- Brand Logo -->
      <div class="flex items-center flex-shrink-0">
        <a href="/" class="flex items-center gap-2 group outline-none focus-visible:ring-2 focus-visible:ring-brand-primary rounded-lg transition-all duration-300">
          <span class="text-xl font-black tracking-tight text-gray-950">
            Gold<span class="text-brand-primary group-hover:text-gray-950 transition-colors duration-300">Plus</span>
          </span>
        </a>
      </div>

      <!-- Integrated Search Bar -->
      <form action="/shop" method="GET" class="flex-grow max-w-md relative group mx-auto">
        <div class="relative w-full">
          <input
            type="search"
            name="q"
            value={q}
            placeholder="Search chargers, earbuds, power banks..."
            aria-label="Product Search"
            class="w-full pl-10 pr-20 py-2.5 bg-gray-50 hover:bg-gray-100/60 focus:bg-white border border-gray-200/80 rounded-xl focus:border-brand-primary focus:ring-4 focus:ring-brand-primary/10 transition-all duration-300 outline-none text-xs placeholder-gray-400 text-gray-800"
          />
          <span class="absolute left-3.5 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-brand-primary transition-colors duration-300">
            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
          </span>
          <button type="submit" class="absolute right-2.5 top-1/2 -translate-y-1/2 px-3 py-1 text-[10px] font-black uppercase tracking-widest text-gray-500 hover:text-gray-950 transition-colors duration-300 outline-none rounded">
            Search
          </button>
        </div>
      </form>

      <!-- Actions -->
      <div class="flex items-center gap-5 flex-shrink-0">
        <a href="/account" class="text-xs font-bold text-gray-600 hover:text-gray-950 transition-colors py-1.5 focus-visible:ring-2 focus-visible:ring-brand-primary outline-none rounded-lg">
          Account
        </a>
        <div class="h-3 w-px bg-gray-200"></div>
        <a href="/cart" class="group flex items-center gap-2.5 px-3.5 py-1.5 bg-gray-50 hover:bg-gray-100/80 border border-gray-100 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary">
          <span class="text-xs font-bold text-gray-705 group-hover:text-gray-950 transition-colors">Cart</span>
          <span class="bg-brand-primary text-gray-950 text-[10px] font-black px-2 py-0.5 rounded-full transition-transform duration-300 group-hover:scale-110">
            {cartItemCount}
          </span>
        </a>
      </div>
    </div>

    <!-- Bottom Row: Centered Segmented Dock -->
    <div class="border-t border-gray-100 bg-gray-50/50 py-3">
      <div class="flex items-center justify-center">
        <nav class="flex items-center bg-white p-1 border border-gray-200/80 rounded-2xl text-xs font-semibold text-gray-700" aria-label="Medium Categories">
          <!-- Shop All Segment (Slate) -->
          <a
            href="/shop"
            title="Browse all GoldPlus products"
            aria-label="Browse all GoldPlus products"
            class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-gray-50 hover:text-slate-900 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
          >
            <div class="w-8 h-8 rounded-lg bg-slate-50 border border-slate-100 flex items-center justify-center text-slate-500 group-hover:bg-slate-100 group-hover:border-slate-200 group-hover:text-slate-700 transition-colors duration-300">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <rect x="3" y="3" width="7" height="7" rx="1.5"></rect>
                <rect x="14" y="3" width="7" height="7" rx="1.5"></rect>
                <rect x="3" y="14" width="7" height="7" rx="1.5"></rect>
                <rect x="14" y="14" width="7" height="7" rx="1.5"></rect>
              </svg>
            </div>
            <span class="text-[13px] font-semibold text-gray-700 group-hover:text-slate-900 transition-colors duration-300">Shop All</span>
          </a>

          <!-- Divider line -->
          <div class="h-4 w-px bg-gray-200"></div>

          <!-- Power Segment (Emerald) -->
          <a
            href="/shop?q=charger"
            title="Power devices: chargers, charging cables and power banks"
            aria-label="Power devices: chargers, charging cables and power banks"
            class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-gray-50 hover:text-emerald-800 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
          >
            <div class="w-8 h-8 rounded-lg bg-emerald-50 border border-emerald-100 flex items-center justify-center text-emerald-600 group-hover:bg-emerald-100/80 group-hover:border-emerald-200 group-hover:text-emerald-700 transition-colors duration-300">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <rect x="2" y="6" width="16" height="12" rx="2"></rect>
                <path d="M22 10v4M10 9l-2 3h4l-2 3"></path>
              </svg>
            </div>
            <span class="text-[13px] font-semibold text-gray-700 group-hover:text-emerald-800 transition-colors duration-300">Power</span>
          </a>

          <!-- Divider line -->
          <div class="h-4 w-px bg-gray-200"></div>

          <!-- Sound Segment (Sky Blue) -->
          <a
            href="/shop?q=earbuds"
            title="Sound devices: earbuds and portable speakers"
            aria-label="Sound devices: earbuds and portable speakers"
            class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-gray-50 hover:text-sky-800 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
          >
            <div class="w-8 h-8 rounded-lg bg-sky-50 border border-sky-100 flex items-center justify-center text-sky-600 group-hover:bg-sky-100/80 group-hover:border-sky-200 group-hover:text-sky-700 transition-colors duration-300">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M3 18v-6c0-4.97 4.03-9 9-9s9 4.03 9 9v6"></path>
                <path d="M21 19a2 2 0 01-2 2h-1a2 2 0 01-2-2v-3a2 2 0 012-2h3v5zM3 19a2 2 0 002 2h1a2 2 0 002-2v-3a2 2 0 00-2-2H3v5z"></path>
              </svg>
            </div>
            <span class="text-[13px] font-semibold text-gray-700 group-hover:text-sky-800 transition-colors duration-300">Sound</span>
          </a>

          <!-- Divider line -->
          <div class="h-4 w-px bg-gray-200"></div>

          <!-- Storage Segment (Amber) -->
          <a
            href="/shop?q=flash"
            title="Storage devices: flash drives, SD cards and external storage"
            aria-label="Storage devices: flash drives, SD cards and external storage"
            class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-gray-50 hover:text-amber-850 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
          >
            <div class="w-8 h-8 rounded-lg bg-amber-50 border border-amber-100 flex items-center justify-center text-amber-600 group-hover:bg-amber-100/80 group-hover:border-amber-200 group-hover:text-amber-700 transition-colors duration-300">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <rect x="6" y="8" width="12" height="11" rx="2"></rect>
                <rect x="9" y="3" width="6" height="5"></rect>
                <path d="M11 5h2"></path>
              </svg>
            </div>
            <span class="text-[13px] font-semibold text-gray-700 group-hover:text-amber-850 transition-colors duration-300">Storage</span>
          </a>

          <!-- Divider line -->
          <div class="h-4 w-px bg-gray-200"></div>

          <!-- Car Segment (Teal) -->
          <a
            href="/shop?q=mount"
            title="Car accessories: phone mounts and car chargers"
            aria-label="Car accessories: phone mounts and car chargers"
            class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-gray-50 hover:text-teal-850 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
          >
            <div class="w-8 h-8 rounded-lg bg-teal-50 border border-teal-100 flex items-center justify-center text-teal-600 group-hover:bg-teal-100/80 group-hover:border-teal-200 group-hover:text-teal-700 transition-colors duration-300">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M7 10l1.3-4.1a1 1 0 011-.9h5.4a1 1 0 011 .9L17 10"></path>
                <rect x="3" y="10" width="18" height="6" rx="2.5"></rect>
                <circle cx="6.5" cy="13" r="0.5" fill="currentColor"></circle>
                <circle cx="17.5" cy="13" r="0.5" fill="currentColor"></circle>
                <rect x="5" y="16" width="2" height="3" rx="0.5"></rect>
                <rect x="17" y="16" width="2" height="3" rx="0.5"></rect>
              </svg>
            </div>
            <span class="text-[13px] font-semibold text-gray-700 group-hover:text-teal-850 transition-colors duration-300">Car</span>
          </a>

          <!-- Divider line -->
          <div class="h-4 w-px bg-gray-200"></div>

          <!-- PC Segment (Indigo) -->
          <a
            href="/shop"
            title="PC accessories: mice and sound cards"
            aria-label="PC accessories: mice and sound cards"
            class="group flex items-center gap-2.5 px-3 py-1.5 hover:bg-gray-50 hover:text-indigo-850 rounded-xl transition-all duration-300 outline-none focus-visible:ring-2 focus-visible:ring-brand-primary active:scale-98"
          >
            <div class="w-8 h-8 rounded-lg bg-indigo-50 border border-indigo-100 flex items-center justify-center text-indigo-600 group-hover:bg-indigo-100/80 group-hover:border-indigo-200 group-hover:text-indigo-700 transition-colors duration-300">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
                <rect x="7" y="3" width="10" height="18" rx="5"></rect>
                <path d="M12 6v4M7 11h10"></path>
              </svg>
            </div>
            <span class="text-[13px] font-semibold text-gray-700 group-hover:text-indigo-850 transition-colors duration-300">PC</span>
          </a>
        </nav>
      </div>
    </div>
  </div>

  <!-- MOBILE LAYOUT (under 768px) -->
  <!-- Mobile Header Banner: GoldPlus | Cart | Menu -->
  <div class="md:hidden">
    <div class="flex items-center justify-between px-4 py-3 bg-white">
      <!-- Brand Logo -->
      <a href="/" class="flex items-center gap-2 group outline-none">
        <span class="text-lg font-black tracking-tight text-gray-950">
          Gold<span class="text-brand-primary">Plus</span>
        </span>
      </a>

      <!-- Right Side Actions: Cart | Menu -->
      <div class="flex items-center gap-3">
        <!-- Cart Badge Link -->
        <a href="/cart" class="relative flex items-center justify-center w-9 h-9 bg-gray-50 border border-gray-100 rounded-xl outline-none">
          <svg class="w-4 h-4 text-gray-600" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
          </svg>
          <span class="absolute -top-1 -right-1 bg-brand-primary text-gray-950 text-[9px] font-black w-4.5 h-4.5 rounded-full flex items-center justify-center">
            {cartItemCount}
          </span>
        </a>

        <!-- Hamburger Drawer Button -->
        <button
          type="button"
          id="mobile-menu-toggle"
          aria-expanded="false"
          aria-controls="mobile-menu-drawer"
          aria-label="Toggle Navigation Drawer"
          class="flex items-center justify-center w-9 h-9 bg-gray-50 border border-gray-100 rounded-xl outline-none focus-visible:ring-2 focus-visible:ring-brand-primary"
        >
          <svg id="hamburger-icon" class="w-4.5 h-4.5 text-gray-600" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
          <svg id="close-icon" class="hidden w-4.5 h-4.5 text-gray-600" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile Horizontal Icon Category Rail -->
    <div class="flex items-center gap-3 overflow-x-auto scroll-smooth snap-x snap-mandatory px-4 py-2.5 bg-gray-50/70 border-t border-b border-gray-100 no-scrollbar">
      <!-- Shop All Segment (Slate) -->
      <a href="/shop" class="snap-start flex items-center gap-2 px-3 py-1.5 bg-white border border-gray-200/70 rounded-xl text-gray-700 text-xs font-bold flex-shrink-0 shadow-sm">
        <div class="w-9 h-9 rounded-lg bg-slate-50 border border-slate-100 flex items-center justify-center text-slate-500">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
            <rect x="3" y="3" width="7" height="7" rx="1.5"></rect>
            <rect x="14" y="3" width="7" height="7" rx="1.5"></rect>
            <rect x="3" y="14" width="7" height="7" rx="1.5"></rect>
            <rect x="14" y="14" width="7" height="7" rx="1.5"></rect>
          </svg>
        </div>
        <span>Shop All</span>
      </a>

      <!-- Power Segment (Emerald) -->
      <a href="/shop?q=charger" class="snap-start flex items-center gap-2 px-3 py-1.5 bg-white border border-gray-200/70 rounded-xl text-gray-700 text-xs font-bold flex-shrink-0 shadow-sm">
        <div class="w-9 h-9 rounded-lg bg-emerald-50 border border-emerald-100 flex items-center justify-center text-emerald-600">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
            <rect x="2" y="6" width="16" height="12" rx="2"></rect>
            <path d="M22 10v4M10 9l-2 3h4l-2 3"></path>
          </svg>
        </div>
        <span>Power</span>
      </a>

      <!-- Sound Segment (Sky Blue) -->
      <a href="/shop?q=earbuds" class="snap-start flex items-center gap-2 px-3 py-1.5 bg-white border border-gray-200/70 rounded-xl text-gray-700 text-xs font-bold flex-shrink-0 shadow-sm">
        <div class="w-9 h-9 rounded-lg bg-sky-50 border border-sky-100 flex items-center justify-center text-sky-600">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
            <path d="M3 18v-6c0-4.97 4.03-9 9-9s9 4.03 9 9v6"></path>
            <path d="M21 19a2 2 0 01-2 2h-1a2 2 0 01-2-2v-3a2 2 0 012-2h3v5zM3 19a2 2 0 002 2h1a2 2 0 002-2v-3a2 2 0 00-2-2H3v5z"></path>
          </svg>
        </div>
        <span>Sound</span>
      </a>

      <!-- Storage Segment (Amber) -->
      <a href="/shop?q=flash" class="snap-start flex items-center gap-2 px-3 py-1.5 bg-white border border-gray-200/70 rounded-xl text-gray-700 text-xs font-bold flex-shrink-0 shadow-sm">
        <div class="w-9 h-9 rounded-lg bg-amber-50 border border-amber-100 flex items-center justify-center text-amber-600">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
            <rect x="6" y="8" width="12" height="11" rx="2"></rect>
            <rect x="9" y="3" width="6" height="5"></rect>
            <path d="M11 5h2"></path>
          </svg>
        </div>
        <span>Storage</span>
      </a>

      <!-- Car Segment (Teal) -->
      <a href="/shop?q=mount" class="snap-start flex items-center gap-2 px-3 py-1.5 bg-white border border-gray-200/70 rounded-xl text-gray-700 text-xs font-bold flex-shrink-0 shadow-sm">
        <div class="w-9 h-9 rounded-lg bg-teal-50 border border-teal-100 flex items-center justify-center text-teal-600">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
            <path d="M7 10l1.3-4.1a1 1 0 011-.9h5.4a1 1 0 011 .9L17 10"></path>
            <rect x="3" y="10" width="18" height="6" rx="2.5"></rect>
            <circle cx="6.5" cy="13" r="0.5" fill="currentColor"></circle>
            <circle cx="17.5" cy="13" r="0.5" fill="currentColor"></circle>
            <rect x="5" y="16" width="2" height="3" rx="0.5"></rect>
            <rect x="17" y="16" width="2" height="3" rx="0.5"></rect>
          </svg>
        </div>
        <span>Car</span>
      </a>

      <!-- PC Segment (Indigo) -->
      <a href="/shop" class="snap-start flex items-center gap-2 px-3 py-1.5 bg-white border border-gray-200/70 rounded-xl text-gray-700 text-xs font-bold flex-shrink-0 shadow-sm">
        <div class="w-9 h-9 rounded-lg bg-indigo-50 border border-indigo-100 flex items-center justify-center text-indigo-600">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" aria-hidden="true">
            <rect x="7" y="3" width="10" height="18" rx="5"></rect>
            <path d="M12 6v4M7 11h10"></path>
          </svg>
        </div>
        <span>PC</span>
      </a>
    </div>
  </div>

  <!-- Mobile Dropdown Drawer Panel -->
  <div
    id="mobile-menu-drawer"
    class="hidden md:hidden border-t border-gray-100 bg-white"
  >
    <div class="px-4 py-5 space-y-6">
      <!-- Mobile Search Form -->
      <form action="/shop" method="GET" class="relative">
        <input
          type="search"
          name="q"
          value={q}
          placeholder="Search chargers, earbuds..."
          aria-label="Mobile Product Search"
          class="w-full pl-10 pr-4 py-2.5 bg-gray-50 border border-gray-100 rounded-xl focus:border-brand-primary outline-none text-xs placeholder-gray-400 text-gray-850"
        />
        <span class="absolute left-3.5 top-1/2 -translate-y-1/2 text-gray-400">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </span>
      </form>

      <!-- Products Navigation Link -->
      <div>
        <a
          href="/shop"
          title="Browse all GoldPlus products"
          aria-label="Browse all GoldPlus products"
          class="w-full text-center py-3 bg-gray-50 hover:bg-gray-100 border border-gray-100 text-brand-primary font-black uppercase tracking-widest rounded-xl text-[11px] block transition-colors duration-300"
        >
          Shop All Products
        </a>
      </div>

      <!-- General Navigation Links -->
      <div class="space-y-4 pt-4 border-t border-gray-100 text-xs font-semibold text-gray-700">
        <a href="/account" class="block hover:text-brand-primary transition-colors">
          Account Profile
        </a>
        <a href="/cart" class="block hover:text-brand-primary transition-colors">
          Shopping Cart
        </a>
      </div>
    </div>
  </div>
</header>

<style>
  /* Custom scrollbar hiding for horizontal category rail */
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
</style>

<script>
  // Mobile drawer interactions
  const toggleBtn = document.getElementById('mobile-menu-toggle');
  const drawer = document.getElementById('mobile-menu-drawer');
  const hamburgerIcon = document.getElementById('hamburger-icon');
  const closeIcon = document.getElementById('close-icon');

  if (toggleBtn && drawer && hamburgerIcon && closeIcon) {
    toggleBtn.addEventListener('click', () => {
      const isExpanded = toggleBtn.getAttribute('aria-expanded') === 'true';
      toggleBtn.setAttribute('aria-expanded', (!isExpanded).toString());
      
      if (isExpanded) {
        drawer.classList.add('hidden');
        hamburgerIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
      } else {
        drawer.classList.remove('hidden');
        hamburgerIcon.classList.add('hidden');
        closeIcon.classList.remove('hidden');
      }
    });

    // Close mobile drawer on any category/nav click
    drawer.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        toggleBtn.setAttribute('aria-expanded', 'false');
        drawer.classList.add('hidden');
        hamburgerIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
      });
    });

    // Close mobile menu on Escape key press
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && toggleBtn.getAttribute('aria-expanded') === 'true') {
        toggleBtn.setAttribute('aria-expanded', 'false');
        drawer.classList.add('hidden');
        hamburgerIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
        toggleBtn.focus();
      }
    });
  }
</script>
