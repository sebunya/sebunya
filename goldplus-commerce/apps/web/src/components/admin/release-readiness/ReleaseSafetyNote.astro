---
---
<div class="bg-blue-50 border border-blue-200 text-blue-800 p-4 rounded-md text-sm mb-4">
  <strong class="font-semibold">Safety Note:</strong> This dashboard executes read-only validation commands (e.g. <code>pnpm run typecheck</code>, <code>vitest</code>, GTM dry-runs). It does <strong>not</strong> push code, publish tags, or override live consent settings.
</div>
