---
import RecommendationRail from "./RecommendationRail.astro";
import { getRecommendations } from "../../lib/recommendations";

interface Props {
  categoryId?: string;
  categorySlug?: string;
}

const { categoryId, categorySlug } = Astro.props;

const response = await getRecommendations({
  placement: "category_popular",
  categoryId,
  categorySlug,
  limit: 8,
});
---

<RecommendationRail
  title="Popular in Category"
  subtitle="Most viewed options recently added by our editors."
  placement="category_popular"
  items={response.items}
/>
