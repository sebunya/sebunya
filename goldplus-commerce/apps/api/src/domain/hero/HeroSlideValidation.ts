/** Re-export from @goldplus/shared — ONE source of truth for hero validation. */
export {
  HERO_LIMITS, sanitiseHeadline, headlineVisibleText, validateHeroSlide, flashSaleHasEnded,
} from '@goldplus/shared';
export type { HeroSlideFieldErrors } from '@goldplus/shared';
