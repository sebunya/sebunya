/** Re-export from @goldplus/shared — ONE source of truth for the hero library. */
export {
  HERO_THEMES, HERO_TINTS, HERO_MEDIA,
  HERO_SLIDE_LIBRARY, HERO_SETTINGS_DEFAULT, HERO_FALLBACK_KEY,
  HERO_SELECTION_TUNING, HERO_SELECTION_RULES, selectHeroSlides,
} from '@goldplus/shared';
export type {
  HeroTheme, HeroTint, HeroMedia, HeroSlideSeed, HeroSettingsSeed,
  HeroSelectionContext, HeroSelectionTuning, HeroSelectable,
} from '@goldplus/shared';
