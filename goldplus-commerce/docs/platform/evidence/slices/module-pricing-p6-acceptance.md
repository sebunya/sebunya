# Pricing P6 — production-shaped acceptance

- Base: clean/pushed P5 route-census head `09ceb5a182acaceb913b5f73844f4844060360c0`; application runtime source is unchanged in P6.
- One fail-fast acceptance runner executes the P1 governance, P2 evaluation, P3 capacity, P4 checkout/payment and P5/P6 operations proofs against real PostgreSQL. Its deterministic final verdict reports five components, controlled-provider-only execution, zero real provider calls, zero proof residue and `PASS`. The operations proof explicitly persists pause → resume → safe terminal pause, verifies restored active capacity and rejects post-draft Experiment association. Protected tests prove resume uses only `pricing.activate` and Automation/Decision Intelligence have no Pricing activation dependency.
- Fresh migration replay applied all 43 migrations through additive `0042`, created all nine Pricing tables and left zero active promotions. A populated `0041` database retained a legacy order and line across `0042`; the order backfilled base subtotal `250000`, zero discount/tax and `legacy-unadjusted-v1`, while its line backfilled canonical unit price `125000`, base/final subtotal `250000` and zero discount.
- Compiled plain-Node execution of `dist/scripts/pricing-operations-proof.js` passed against PostgreSQL with lifecycle, Experiment association, canonical simulation, zero quote/reservation/redemption/order/payment/outbox deltas, zero provider calls and zero residue.
- Clean source from exact commit `09ceb5a` was materialized with `git archive` and built on the Docker daemon reachable only through `ssh goldplus-prod`; no production checkout was switched and no Compose command ran. The pinned Node base is `node:20-alpine@sha256:fb4cd12c85ee03686f6af5362a0b0d56d50c58a04632e6c0fb8363f609372293` on Linux/amd64.
- API image `sha256:80e45db79b37abd1bda6a1d5a3f9f4dd03244ed4dfa14e643e1f6c86504eead5` and web image `sha256:634aae1245e6bcd8e81e3a0adeb6f6ac045c337e3a52a24e942189f1290551b2` both carry revision `09ceb5a182acaceb913b5f73844f4844060360c0`.
- The API image-start smoke used `--network none`, a read-only root filesystem and plain Node. `/health/live` returned 200 and `/admin/pricing/overview` returned 401 logged out. The web image used the same isolation; `/` returned 200 and `/admin/pricing` returned 303 to the administrator login route.
- Before/after snapshots prove all seven running production container IDs were unchanged. Ephemeral smoke containers were removed. Network isolation plus PostgreSQL counter proofs establish zero real provider/customer communication activity; the acceptance runner removed all scratch business data.
- Gates: five real-PostgreSQL acceptance components pass; fresh replay and populated upgrade pass; production API/web builds and isolated image starts pass; Pricing plus architecture, workspace typecheck/build, secret scan, changed-path lint and diff check pass. The dirty-tree full suite had 4,037 behavioral passes and only nine historical artifact-scope guard failures; the clean P6 commit rerun passes 198 files / 4,047 tests. Repository-wide lint retains only the established unrelated `ICustomerDnaRepository.ts:6` baseline error.
- Status: `SOURCE_COMPLETE_NOT_DEPLOYED`. P7 must freeze and rebuild one exact executable release candidate, prove database-connected production-image startup, verified backup/restore and rollback readiness. No migration, deployment, production promotion activation, order, payment, reservation, redemption, provider call or customer communication occurred in P6.
