# Slice 10-AB2 APEX Ring 2 public block proof

Ring 2 is read-only and receives no canonical write authorization. Anonymous, non-allowlisted, checkout-only, support-only, legacy and unknown-imported identities are rejected by the pure server-side guard. Failed saves return `saved=false`; no event or projection is created.
