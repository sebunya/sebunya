# Slice 10-D DEPLOY R2 PERFECT artifact review

The local slice delta is restricted to this 12-document deployment evidence set and `NEXT_WORKTREE_README.md`. There is no runtime application, Dockerfile, Compose, Caddyfile, consent, provider, checkout/payment/order, auth/RBAC, Credential Vault, migration, Measurement activation, loyalty/reward/personalisation, environment, or secret change.

Approval, lock, evidence-only source gap, fresh source preservation, fresh rollback tags, identical read-only counters, exact-head fast-forward, Compose validation, API/web build, and isolated API image-start smoke passed. The target replicas initially became healthy and the protected routes responded correctly, but both API replicas later exited during the health window. The bounded failure evidence identifies a compiled database-client runtime incompatibility and a subsequent unhandled PostgreSQL UUID-input rejection. Fresh image rollback completed successfully; all rollback replicas and public health checks are green.

Artifact decision: `SLICE_10_D_DEPLOY_R2_PERFECT_ROLLED_BACK_AFTER_HEALTH_FAILURE`. Production evidence-head reconciliation completed only after its diff was proven docs/`NEXT_WORKTREE_README.md`-only; it did not trigger a build or restart. The Control Room is not live.
