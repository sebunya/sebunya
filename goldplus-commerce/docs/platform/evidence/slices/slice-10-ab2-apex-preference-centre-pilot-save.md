# Slice 10-AB2 APEX Preference Centre pilot save

The Ring 1 guard passed with verified identity, allowlist membership, canonical purpose/channel, Preference Centre source, copy version, correlation ID, idempotency key, audit requirement, provider-live-send false and cooldown bypass false.

Correlation: `slice-10-ab2-ring1-save`. The canonical grant persisted, replay returned `already_applied=true` with the same event ID, withdrawal persisted, projection ended `withdrawn`, and the eligibility preview was ineligible after withdrawal. No provider transport was invoked.
