# Pricing P4 — authoritative checkout and payment integrity

- Base: clean/pushed P3 `8627cdb6fa304804ce885ad00cf2bd21132eb398`; clean repository suite green.
- Production checkout now composes the existing Pricing evaluator, quote repository and capacity boundary. The request supplies product IDs, quantities and optional coupon/preview acknowledgement only; canonical catalogue prices and approved immutable versions are reloaded server-side.
- A changed preview returns `PRICE_CHANGED` or `PROMOTION_CHANGED` before reservation or order creation unless the revised breakdown is explicitly accepted. Customer DNA and Experiment evidence are not accepted from the public browser path, so unsupported eligibility fails closed.
- One repository-native transaction persists the order, order lines, full pricing snapshot, quote/version provenance, reservation redemptions and reservation status transition. Persistence failure rolls back the order transaction and the checkout use case compensates live reservations. Once an order commits, its redemption remains attached to that business fact; downstream Inventory backorder handling does not erase a valid commercial redemption.
- Immutable order evidence contains canonical unit price, quantity, line base/discount/final amounts, adjustments, definition/version IDs, safe coupon reference, Experiment evidence, calculation version, currency, final totals, shipping, tax and evaluation time.
- PesaPal initiation creates its attempt from the committed order total. Every retry submits the immutable attempt amount/currency. Redirect remains pending; authoritative callback/IPN verification checks reference, amount and currency, and payment status updates do not change pricing evidence.
- Real PostgreSQL proof: browser price `1` produced canonical unit price `100000`; approved 10% promotion produced order total `180000`; exactly one order, reservation and redemption committed; client-key replay reused the order; forced pre-order persistence failure released its reservation; two controlled PesaPal submissions both used `180000`; callback retained the snapshot; outbox delta, real-provider calls and proof residue were zero.
- Gates: Pricing plus architecture 25/25; legacy checkout/payment plus focused Pricing 56/56; workspace typecheck/build, secret scan (1,155 files), changed-path lint with zero errors and `git diff --check` pass. Migration 0042 and migrations 0000–0041 are unchanged.
- Status remains `SOURCE_PARTIAL`; P5 protected Pricing Operations Control Room is next. No production migration, deployment, live provider call or customer communication occurred.
