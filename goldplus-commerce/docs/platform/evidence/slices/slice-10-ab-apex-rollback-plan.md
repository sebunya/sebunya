# Slice 10-AB APEX rollback plan

If the scoped API overlay must be reverted, restore the source archive and recreate only the API containers. Consent UAT rows are identifiable by the synthetic identity prefix and correlation recorded in the evidence; remove only those rows through the existing audited operational procedure if required. Do not roll back unrelated consent history or run a migration.
