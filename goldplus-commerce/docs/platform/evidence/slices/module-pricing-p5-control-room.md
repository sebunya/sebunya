# Pricing P5 — Pricing Operations Control Room

- Base: clean/pushed P4 `febdb9850d38d4433bdc7111124038f0ee94580c`; clean suite 197 files / 4,041 tests.
- Added exact permissions `pricing.read`, `pricing.create`, `pricing.manage`, `pricing.approve`, `pricing.activate`, `pricing.simulate` and `pricing.pause`. Read, approval, activation, pause and simulation are separately protected; the pre-existing `pricing.manage` remains the version/configuration boundary.
- Protected API exposes persisted definition/version detail, approval decisions, Experiment associations, shared audit events, quote/redemption totals, reservation states and active-version capacity. Metrics are database aggregates; no static usage or provider status is supplied.
- Every commercial mutation delegates to the existing Pricing governance or audited association use case. Experiment association is accepted only for a DRAFT immutable version and an existing governed READY/RUNNING/PAUSED Experiment variant.
- The Astro index and detail pages expose draft creation, versioning, conditions, benefits, exclusions, schedule, limits, coupon, submission, approval/rejection, activation, pause/resume, archive, safe simulation, decision trace, reservations/redemptions/capacity, Experiment evidence and audit. Loading, empty, unavailable, permission-denied and stale-conflict states are explicit.
- Simulation always forces `persist:false`; it can read canonical catalogue and approved rules but does not call reservation, order, payment, outbox or provider boundaries.
- Real PostgreSQL proof: DRAFT → READY_FOR_REVIEW → APPROVED → ACTIVE → PAUSED; one governed Experiment association; canonical base `200000`, simulated discount `30000`, final `170000`; truthful active capacity then paused removal; one approval and five pre-pause audits; zero quote/reservation/redemption/order/payment/outbox delta, zero provider calls and zero residue.
- Gates: Pricing plus architecture 30/30, workspace typecheck/build, secret scan (1,162 files), changed-path lint with zero errors and diff check pass. Repository-wide lint remains a pre-existing unrelated baseline error at `apps/api/src/application/ports/ICustomerDnaRepository.ts:6`.
- Status remains `SOURCE_PARTIAL`; P6 production-shaped acceptance is next. No migration changed and no production deployment, real provider call or customer communication occurred.
