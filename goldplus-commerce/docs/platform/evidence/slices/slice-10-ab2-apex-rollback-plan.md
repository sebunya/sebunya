# Slice 10-AB2 APEX rollback plan

Restore the verified source archive and recreate only API containers if the scoped overlay requires rollback. The synthetic Ring 1 consent lifecycle is identified by its recorded correlation and synthetic identity hash; remove only those records through the existing audited operational procedure if necessary. Do not roll back unrelated consent history or run a migration.
