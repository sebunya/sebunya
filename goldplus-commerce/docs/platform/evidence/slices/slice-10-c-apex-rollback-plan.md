# Slice 10-C APEX rollback plan

Disable the pilot ring through the cohort control, then recreate only API containers from the verified source archive if the overlay must be reverted. Cohort lifecycle records are identified by their synthetic hashes and correlation IDs; remove only those records through the existing audited operational procedure if necessary. No migration is required.
