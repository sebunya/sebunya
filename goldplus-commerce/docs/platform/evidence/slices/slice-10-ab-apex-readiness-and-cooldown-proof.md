# Slice 10-AB APEX readiness and cooldown proof

The 9-ZI cooldown state remains `unknown`, `safe_to_attempt_now=false`, with one deferred diagnostic budget remaining. Consent UAT uses provider dry-run eligibility only; after withdrawal, eligibility is false and delivery mode is disabled. No provider transport is reachable through the pilot guard.
