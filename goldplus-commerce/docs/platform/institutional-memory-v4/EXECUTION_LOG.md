# Execution log — institutional-memory advancement (V5 kernel)
Format: timestamp · phase · event · evidence.

- 2026-08-03 · P0 · Programme V5 ingested; attestation recorded (Fable 5 / max / thinking on; runtime `claude-fable-5`). Anchors verified: branch clean at `707876d`; gh unauthenticated (PR body edits blocked, pushes still update PR #9).
- 2026-08-03 · P0 · Prior-programme lineage adopted without re-derivation: P0-2+U1–U6 green; platform-wide recovery RC-1…RC-7 fixed+verified live (see docs/production-recovery/2026-08-03-platform-wide/).
- 2026-08-03 · P0 · Three parallel read-only recon agents launched to build ROUTE_CAPABILITY / PERMISSION_ROUTE / SCHEMA_OWNERSHIP / WORKER_EVENT / UI_API_CONTRACT / TEST_COVERAGE indexes.
- 2026-08-03 · P0 · Core memory docs authored: INSTITUTIONAL_MEMORY, CANONICAL_OWNERSHIP, DO_NOT_BREAK_LEDGER (+json), EXECUTION_STATE, NEXT_RESUME.
- 2026-08-03 · P0→P1 · Gap slices selected (gap-only, F7): S1 governed-admin permission sync; S2 cart 180d retention; S3 regression guards (cart unit tests + SSR-origin architecture guard); S4 maturity matrix from indexes; S5 reversible release + proof.
- 2026-08-03 · P1 · GREEN checkpoint 1: S1 governed-admin sync (advisory-locked, add-only, audited; PLATFORM_ADMINISTRATOR full-grant + Owner top-up + bootstrap-admin assignment, wired at boot) · S2 cart retention 30d→180d via single shared CART_RETENTION_DAYS · S3 regression guards (16 tests: registry contract, MutateCartUseCase RC-7 suite, RC-6 parse, RC-1 architecture guard). Focused gate: 16/16 + shared/api/web tsc clean.
- 2026-08-03 · P0 · Recon delivery (schema/worker agent): 204 tables (36 orphaned — no reader/writer: campaigns, utm_links, wishlists, product_feeds, review_media, creator_contracts, most measurement_* advanced, …), 9 BullMQ queues (4 declared without worker/producer: telemetry-replay, inventory-sync, abandoned-cart-events, recommendation-materialization), outbox partitioned 3-way (general / checkout side-effects / telemetry) with 8-attempt dead-letter + dedicated telemetry_dlq (5 attempts, 30s→6h backoff, replayable). Local journal 76 migrations (0000–0075) — reconcile vs prod count at release parity.
- 2026-08-03 · P1 · Recon delivery (route/permission agent): 55 mounts, 360 handlers, 84 permission constants, 271 guarded routes (2 step-up MFA), 14 constants guarding no route. NO production role bootstrap existed before S1 (acceptance-bootstrap refuses prod; roles GET-only) — S1 is the first code-driven path. Defects found → S6.
- 2026-08-03 · P1 · Recon delivery (UI/test agent): 91 pages indexed; 6 measurement pages call the same-origin BFF proxy (by design — proxy attaches bearer) with one real drift (control-tower hand-builds base from PUBLIC_API_URL); 322 test files, all 10 checklist domains covered; gaps: apps/web + packages/shared have no in-package tests (root suites cover), api has no in-app integration tier (root tests/integration covers).
- 2026-08-03 · P1 · GREEN checkpoint 2 (S6 route-layer hardening): controlled-activation-live-review was a fail-closed DEAD surface (presentation/ router, no auth chain, adminId never set → 401 for legitimate admins) — now authMiddleware + method-shaped permission gate (GET=reports.read, mutations=settings.manage) + adminId mapping, mirroring the dry-run sibling; dead duplicate mount of measurement-payments removed (app.ts mount is canonical); '/admin/modules' added to MOUNTED_API_PREFIXES. Gate: api tsc 0, architecture 97/97 then focused 25/25.
- 2026-08-03 · P1 · S5 RELEASED at `473ede0`: prod build api+web (env-file), rolling up, 4 replicas healthy, ready=200. S1 first live convergence caught REAL drift: permissionsInserted=1, rolesCreated=11, adminGrantsAdded=84 (PLATFORM_ADMINISTRATOR complete), legacyGrantsAdded=1 (Owner was short one code), bootstrapAdminAssigned=true, 8 beyond-registry rows reported-not-deleted. S2 live: __Host-gp_cart Max-Age=15552000 (180d). S6 live: live-review unauth 401 / authed 200 (was dead-for-everyone); payments router root 200 after dedupe; admin sweep 0/12 fails; funnel add 303 + price renders. Rollback: goldplus-commerce-api:rollback-imv4-pre + prior web tags.
- 2026-08-03 · P1 · S4 GREEN: MODULE_MATURITY_MATRIX, UI_PRODUCT_DEBT_REGISTER, CROSS_MODULE_DEPENDENCY_MAP (+ json mirrors of all memory artifacts). Checkpoint 3 closes the session's engineering; next slices queued in EXECUTION_STATE.nextExact.
- 2026-08-03 · W2A · GREEN checkpoint: control-tower origin/auth/classification repaired (was live silently-broken: rogue env var → localhost SSR fetch → SERVICE_UNAVAILABLE forever behind a 200); 3 new architecture guards (origin allowlist, web→API mapping — 100% of ${apiBase} calls target mounted prefixes, nav-link resolution); 3 real dead links fixed incl. customer-facing PDP support links; route-contract now derives admin routes from disk + asserts cart-credential mint. Slice 8-B1 sweep inventory repaired (84→85, newcomer passes deny-by-default). Gates run: tsc ×3 clean, architecture 101/101, root units 4957 green, api units 224 green, lint warnings-only. Deferred to release boundary: PG/Redis integration (no 2A surface touches them), security scan (no dep changes), Playwright route-contract runs against prod post-roll.
- 2026-08-03 · W2A · RELEASED at `8d06197` (web roll): control-tower live-verified rendering data panels (was SERVICE_UNAVAILABLE forever); PDP support links fixed live; Playwright route-contract vs production exit 0 (full auto-enumerated admin sweep + public + cart-cookie assert, chromium-desktop, 2.1m).
- 2026-08-03 · W2B · Engineering GREEN at `d074c36`: DAM built on the canonical image owner. ROOT CAUSE preserved in memory: product-image uploads wrote to an ephemeral container FS with nothing serving /uploads (write-only black hole). Fix: shared media_uploads volume (api rw, caddy ro) + Caddy /uploads/* file_server with immutable caching + MEDIA_STORAGE_ROOT. New: media_assets/variants/usages schema (migration 0076, journal idx 76), MediaLibraryUseCase (dedup/safe-delete/assign), SharpVariantGenerator (lazy, never gates upload), /admin/media API (media.read|manage, audited, 409 ASSET_IN_USE), /admin/media page + MediaPicker (agent-built, principal-reviewed: local-only return validation, Bearer-only fetches). Gates: media 4/4, arch 101/101, tsc ×3, compose valid, 8-B1 sweep 33/33 @86 pages.
- 2026-08-03 · W2B · RELEASED at `d074c36`: rehearsal on restored clone PASSED (RC=0, 3 media tables + asset_id col) → live migration 0076 complete (LIVE_MEDIA_TABLES=3) → api+web+caddy rolled, 4 replicas healthy, ready=200. PUBLIC PROOF: upload 200 → served via edge `HTTP/2 200, cache-control: immutable` at /uploads/assets/... → identical re-upload deduplicated:true → sharp active (dims detected; variants honestly skipped for 1×1 test, no upscaling) → safe-delete → edge 404. Boot sync auto-granted media.read/manage (guard passed). LESSON (ledger #21): named media volume must be uid 1000 (api runs as node); one-off `chown -R 1000:1000` persists on the volume — same EACCES class as the migration-container lesson.
- 2026-08-03 · W2C · RELEASED at `5c15118`: rehearsal 0077 RC=0 (2 legal tables on clone) → live migration complete → api+web rolled, 4 healthy, ready=200. PUBLIC PROOF: 12 policies ensured; draft→IN_REVIEW live; maker/checker ENFORCED live (403 MAKER_CHECKER on self-approval); drafts never public (/legal/* 404 until published); public pages keep truthful interim static wording as fallback; /admin/legal workspace 200. Old static /admin/legal.astro DEPRECATED_WITH_PROOF (zero inbound links; function fully covered; 86-page sweep green). OPERATIONAL NOTE (honest): publishing requires a second reviewer account — the author can never self-approve; single-admin production therefore cannot publish until a reviewer account exists (user-management is remaining §6 scope). This is governance working, not a defect.
