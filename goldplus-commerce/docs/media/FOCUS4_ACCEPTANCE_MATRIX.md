# Focus 4 — acceptance matrix

Evidence levels: **UNIT** (vitest, pure), **REAL-PG** (`tests/integration/ProductMediaMutation.integration.test.ts` on a disposable production clone on the host, 2026-09-21, 9/9), **BROWSER** (Chromium 1.61 via Playwright against a local storefront + `tests/e2e/focus4/stub-api.mjs` fixtures, `docs/media/evidence/focus4-local-2026-09-21/evidence.json`), **ARCH** (architecture suite), **NOT DONE** (stated plainly).

| Area | Required assertion | Evidence | Status |
|---|---|---|---|
| Cover vs active | Selecting slot 3 changes only presentation; DB, cards, OG, JSON-LD stay on slot 1; no media write request | BROWSER: selection issues only image GETs (`selectUnique`), the page's `og:image`/JSON-LD are rendered server-side from slot 1; the gallery has no write path | PASS |
| Cover change | Atomic C/A swap keeps all assets; projection + usages follow | REAL-PG `set-as-cover swaps slots atomically`; UNIT `ProductMediaSlotMap` | PASS |
| Constraints | Reject slot 0/5, duplicate slot, duplicate asset, unauthorised/unready asset, invalid references | REAL-PG (CHECK + partial indexes + `ASSET_NOT_READY`); UNIT `validateSlotMap` | PASS |
| Concurrent edits | Two writers with one revision cannot both win; empty-gallery concurrent insert yields one cover; replacement; undo revision-checked | REAL-PG `two writers…`, `concurrent insertion…`, `undo…` | PASS |
| Counts / gaps | 0–4 and sparse slots: correct ordinal, previews, finite controls, fallback | UNIT `ProductGalleryState`, `GalleryResolver`; BROWSER one-image (no previews, arrows disabled), no-image fallback | PASS |
| Focus / input | Preview removal never strands focus; Left/Right/Home/End local; modified clicks native; swipe thresholds | BROWSER `keyboard.afterEnter.focusedIs = controls`, End/Home, no wrap; swipe: UNIT-level logic only | PASS (swipe on a real touch device: **REAL_DEVICE_UNVERIFIED**) |
| Loading races | Slow B then fast C ends on C; failed C retains prior; unmount cancels | UNIT `ProductGalleryState` (14 tests) | PASS (unit) |
| Reversed intent / focus | A → pending B → A retains A; tab-away not refocused | UNIT; BROWSER `tabAwayNotPulledBack = true` under 1.5 s latency | PASS |
| Failure separation | Failed thumb keeps full image selectable; failed cover keeps secondaries; transient errors keep count | BROWSER `thumbFailure.selectedAfterThumbFailure = 3 / 4`; UNIT failed-cover case | PASS |
| Purchase hierarchy | GP03BT fixture: action visible at 1440×900; at 390×844 before description/spec table | BROWSER: Add to cart top **675 px** at 1440×900 (baseline 1700); **1118 px** at 390×844, above the spec table (baseline 2235) | PASS (fixture copy; real-product exceptions documented in the release report) |
| Multi-upload | Four files reviewed together; fifth reported; partial failure preserves staged files; stale Save cannot change assignments | API route logic + UNIT `UploadProductImagesUseCase`; admin page tested by build only | PASS (logic) / **NOT DONE** (browser walk of the admin editor) |
| No-JS | Cover/content and buying fallback remain; preview links; inert controls absent | BROWSER `nojs` | PASS |
| Network | Cold load = cover + 3 thumbs; intentional selection = one large; no hidden eager downloads | BROWSER `coldUnique` = cover + 3 thumbs, `coldHasLargeSecondary = false`, `selectLoadedThirdLarge = true`, `selectLoadedNoOtherSecondaryLarge = true`, cover thumb loaded on demotion; the extra cover-large download comes from the recently-viewed rail (`coverImgsOutsideGallery = 1`), not the gallery | PASS |
| Accessibility | axe gate + keyboard/focus, contrast, targets, zoom/reflow, reduced motion | BROWSER axe (wcag2a/aa/21a/21aa) on the gallery: **0 violations**; targets ≥44 px (64–84 px previews, 44 px arrows); reduced-motion CSS; 200 %/400 % zoom **NOT DONE**; screen-reader spot check **NOT DONE** | PARTIAL |
| Migration | Empty, legacy, partial, broken legacy, rerun, concurrent operator edit | UNIT `BackfillProductMediaSlots` (6); REAL-PG migration objects present | PASS (dry run on production: **NOT DONE**, needs the owner's go) |
| Import | Valid names, unknown/ambiguous SKU, slot 5, duplicates, manifest conflicts, replacements, stale revisions, repeat apply, crash/resume, recovery | UNIT `MediaImportPlanner` (9), `MediaImportUseCases` (5: four eyes, blocked plan, stale product, stop-and-resume, repeat refused) | PASS (unit) |
| Operations | Completeness filters; realistic 180-file fixture batch with understandable exceptions | UNIT 180-file batch: 175 NEW, 5 exceptions each naming file + reason | PASS |
| Security | Unauthorised mutation, forged references, invalid files, oversized decoded images, dangerous filenames rejected | ARCH admin-route-authentication (all admin routers); magic-byte type check + 15 MB (existing); unsafe filenames refused in the planner (UNIT); **pixel/decompression cap NOT ADDED** (pre-existing gap) | PARTIAL |
| Cache / history | Post-commit failure/retry; stale job ordering; historical snapshot preservation; rollback after cover swap | No app cache job exists (D5); orders/carts store no image (D6); rollback = projection kept in sync (REAL-PG asserts `image_url` follows slot 1) | PASS by design |
| Commerce | Existing purchase, price, stock, search, recommendation tests remain valid | Full vitest run after each phase: 500 files / 8,168 tests, 0 failures (dirty-tree guards pass on a clean tree) | PASS |
| Analytics | Consent denied still works; no rerender storm | No new event added (deferred); the gallery emits nothing | N/A |
| Responsive widths | 320…1920 automated overflow/geometry; curated visuals at 390/820/1440/1920 | BROWSER: `widths with horizontal overflow: []` for both fixtures at all 14 widths; PNGs at the four curated widths, one- and four-image states, secondary-active, no-JS, thumb-failed | PASS |
| Browser engines / devices | Chromium, Firefox, WebKit; real devices | Chromium only. Firefox/WebKit **NOT RUN** (binaries not installed here). Real devices **REAL_DEVICE_UNVERIFIED** | PARTIAL |

Budgets (provisional, from the brief): gallery controller **3.9 KiB gzip** (≤10), gallery CSS **~1.0 KiB gzip** (≤5), thumbnails: 160 px webp renditions (production examples 3–6 kB), cover: the existing 1024 px `pdp.webp` (GP03BT: 115 kB source, rendition smaller). Five-run cold-load medians on production: **NOT DONE** (no deploy).
