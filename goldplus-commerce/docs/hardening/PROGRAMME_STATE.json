{
  "programmeVersion": "goldplus-hardening-2026-07-29",
  "repository": "sebunya/sebunya",
  "branch": "claude/amazon-grade-module-hardening-20260729",
  "baseCommit": "5e6d4ea37d5f181fbf0ab3a69a8cdb29ad6ca465",
  "currentWave": 1,
  "currentModule": "checkout",
  "completedModules": [
    "authentication",
    "authorization",
    "audit",
    "health-liveness-readiness",
    "database-resilience",
    "outbox",
    "webhooks-payments",
    "inventory",
    "notifications-outbound-gating",
    "loyalty-ledger-integrity",
    "checkout",
    "cart",
    "outbound-delivery"
  ],
  "inProgressModules": [],
  "blockedModules": [
    {
      "module": "release-readiness",
      "reason": "Requires a real macOS host for Rail B validation; unavailable in this Linux sandbox."
    },
    {
      "module": "controlled-activation",
      "reason": "Requires the human approval marker, which must not be created by an agent."
    },
    {
      "module": "production-deployment",
      "reason": "Requires ssh goldplus-prod and production credentials; unavailable here."
    }
  ],
  "openCriticalFindings": [],
  "openHighFindings": [
    {
      "id": "H-01",
      "module": "inventory",
      "summary": "products_reserved_within_stock remains NOT VALID until the readiness gate is run against production data.",
      "gate": "scripts/db/inventory-constraint-readiness.sh"
    }
  ],
  "resolvedFindings": [
    "outbox retry had no jitter (thundering herd on a recovering dependency)",
    "outbox exhaustion recorded as 'processed', indistinguishable from delivery",
    "outbox completion had no compare-and-set; a stale worker could overwrite its successor",
    "inventory lock order was documented but not implemented (deadlock to silent oversell)",
    "inventory stock invariant enforced nowhere; products carried zero CHECK constraints",
    "inventory reservation was best-effort; technical failure became an ordinary backorder",
    "inventory stock update was read-then-write (TOCTOU)",
    "client identity derived ad hoc in nine places; cf-connecting-ip was forgeable",
    "ip-unknown was one shared bucket (accidental global denial of service)",
    "abuse controls were per-replica in-memory Maps",
    "audit_logs was fully mutable by the application role",
    "readiness treated Redis as fatal, pulling the whole fleet on a cache blip",
    "database deadlock retry ran inside the aborted transaction and could never succeed",
    "slow-query log wrote bound parameter values (customer PII)",
    "payment webhook signatureVerified never gated anything (CRITICAL)",
    "checkout idempotency key was globally unique and returned another customer's order",
    "checkout ownership derived from caller-supplied email/phone",
    "checkout idempotency was read-then-write; concurrent submissions both did commerce work",
    "checkout response spread the domain order, publishing customer PII",
    "notification safety preflight returned PASS while unsafe",
    "checkout principal was minted by the API, whose Set-Cookie never reaches the browser through SSR \u2014 every request got a new identity so the claim could never dedupe",
    "authenticated customer session was never applied to the Commerce checkout route",
    "legacy contact-scoped idempotency ran alongside the principal-scoped claim (two contradictory ownership models)",
    "idempotency lease had no fencing token; a stale worker could overwrite its successor",
    "order was persisted before any durable link to the idempotency record",
    "checkout page read res.data.id while the DTO returned orderId \u2014 PesaPal handoff silently never started",
    "checkout key was regenerated on every render, so only a same-render double-click was protected",
    "reservation used an inverted stockHeld variable",
    "migration 0058 initially used gen_random_bytes (pgcrypto), leaving the migration part-applied",
    "takeover did not extend the lease in the same statement, so six concurrent takeovers produced six owners",
    "route imported a repository type directly, violating the architecture boundary",
    "checkout intent was resolved only inside the POST branch, so the initial GET issued no cookie and the template binding was a ReferenceError \u2014 the page was broken on first load while pnpm build passed",
    "operation identity mixed in a caller-controlled hidden clientOrderKey field",
    "operation identity used ambiguous space-joined concatenation instead of a length-prefixed encoding",
    "every fenced mutation result was ignored, so the fence detected lease loss and the caller discarded the detection",
    "order insert and claim link were two statements, leaving a crash window that orphaned orders",
    "checkout request boundary was typed unknown on both customerDetails and items",
    "storefront had no canonical authenticated-session helper, so a signed USER intent alone could outlive a logout",
    "checkout orchestration lived in the HTTP route (289 lines), so transaction and recovery policy could only be exercised through HTTP",
    "expected commerce decisions were identified by parsing error-message prefixes rather than typed outcomes",
    "a retry after a partial success restarted the workflow instead of resuming from the durable order",
    "fulfilment and notification failures were swallowed by console.error, leaving an order with no work item and nothing to alert on",
    {
      "id": "H-04",
      "module": "checkout",
      "resolution": "Closed at a37fc2a. Fingerprint now covers delivery address and normalised contact phone/email, with a length-prefixed canonical encoding (a NUL separator is ambiguous once free text is included) and CHECKOUT_POLICY_VERSION bumped to checkout-v3-full-fingerprint so v2 values are unmatchable by construction. 18 behavioural tests."
    },
    {
      "id": "H-06",
      "module": "checkout",
      "resolution": "Closed at a37fc2a. purgeExpired is state-aware: a row is removable only when its stage holds nothing unresolved, matching migration 0059 partial index; retained rows are counted and returned. Proven against real PostgreSQL \u2014 an expired checkout mid-payment is retained, an expired resolved one is still reclaimed."
    },
    {
      "id": "H-07",
      "module": "checkout",
      "resolution": "Closed at a37fc2a. Cross-site refusal on the checkout POST, evaluated BEFORE the intent is resolved because resolving mints. Sec-Fetch-Site primary (needs no configuration), Origin then Referer fallback against PUBLIC_SITE_ORIGINS, fail-closed on no evidence. 30 behavioural tests."
    },
    {
      "id": "H-08",
      "module": "checkout",
      "resolution": "Closed at c0b8ce6. StartOrderPaymentUseCase authorizes against the checkout that produced the order, reuses a live provider attempt instead of re-submitting, records PAYMENT_STARTED durably plus an ORDER_PAYMENT_VERIFICATION_REQUIRED side effect, and returns typed outcomes so no internal message or blanket 400 reaches the caller. 22 behavioural tests + real-PostgreSQL ownership and forward-only stage proofs."
    },
    {
      "id": "H-02",
      "module": "cart",
      "resolution": "Closed at b1364e6. Cart routes take the cart id from a SIGNED CREDENTIAL, never from the request; the id lives inside the signed claims. Ownership is bound and cross-checked against the verified session; a foreign cart is reported as CART_NOT_FOUND so the endpoint is not a cart-id oracle. GET /carts/:id removed (it put the id in a URL path); the admin lookup moved to GET /admin/carts/:id behind ORDERS_READ, which the customer route never had. Migration 0060 adds owner, version, expiry, quantity bounds and one-row-per-(cart,product). 44 behavioural tests + 25/25 real-PostgreSQL checks."
    },
    {
      "id": "H-03",
      "module": "outbound-delivery",
      "resolution": "Closed at c2058c7. One pure OutboundGovernancePolicy used by email and SMS: ordered guards, typed decisions, each naming its guard as a code. A dry run now reports DRY_RUN rather than SENT \u2014 both adapters previously recorded a simulated message as delivered. PROVIDER_DELIVERY_ENABLED and CUSTOMER_COMMUNICATIONS_ENABLED are now actually enforced; they were read by neither adapter. A contradictory configuration BLOCKS and fails release readiness rather than returning WARN. 47 behavioural tests over the pure policy."
    },
    {
      "id": "4.6-RESIDUAL",
      "module": "checkout",
      "resolution": "Closed at a9756e7. ReconcileOrderPaymentUseCase settles both the browser callback and the IPN through one path, advancing the saga to ORDER_CONFIRMED (nothing did before, so a paid order sat at PAYMENT_STARTED forever). Unverified or unrecognised payments progress nothing and park at PAYMENT_REVIEW. The checkout intent is no longer cleared at the payment handoff, so a lost redirect or a return from the provider reaches the same operation instead of creating a second order."
    },
    {
      "id": "4.7-RESIDUAL",
      "module": "checkout",
      "resolution": "Closed at 2912482. Personal-data fingerprint fields are HMAC digests under a server-held key (an unkeyed hash of a phone number is guessable and allows re-identification). Cart id/version, payment method and the pricing/promotion/stock/delivery policy versions are now fingerprint inputs. CHECKOUT_POLICY_VERSION bumped to checkout-v4-keyed-fingerprint."
    }
  ],
  "migrationCeiling": "0060_cart_ownership_and_version.sql",
  "testTotals": {
    "unit": 4692,
    "unitFiles": 238,
    "architecture": 90,
    "realPostgresChecks": {
      "checkoutDurability": 38,
      "cartAuthorization": 25
    },
    "endToEndChecks": 43,
    "note": "tests/unit is fully green on a clean tree. The 12 Slice-09 artifact-scope guards read git status --porcelain and fail while the tree is dirty by design."
  },
  "lastCleanSuiteCommit": "2912482d34187c5aaad02c7c58590ba5dfe69624",
  "latestReleaseIdentity": {
    "releaseId": "goldplus-hardening-milestone-20260730-1167b3db3055",
    "executableCandidate": "1167b3db30557185b6371df64bad96c67bac3432",
    "targetBranch": "claude/amazon-grade-module-hardening-20260729",
    "migrationCeiling": "0060_cart_ownership_and_version.sql",
    "cloudScopeSha256": "2c8268bc8a901c7bd018f009517eb69e0793372b468ac39c5e062fdedbd336fc",
    "releaseScopeFile": "docs/platform/releases/claude/CLAUDE_HARDENING_RELEASE_SCOPE.json",
    "cloudGate": "PASSED \u2014 thirteen commands, each exit 0, on a clean tree at the candidate",
    "state": "FROZEN_AWAITING_MAC_RAIL_B",
    "controller": "scripts/release/claude/mac-rail-b-hardening-milestone.sh",
    "identity": "docs/platform/releases/claude/HARDENING_MILESTONE_RELEASE.json",
    "supersedes": "02320bb0bdc3df32a42e2060b9f9093c2498e6cb \u2014 re-frozen after a demonstrated failing Mac gate (release-scope drift) was fixed",
    "note": "Image digests, the production backup hash, Playwright evidence and rollback image ids are absent by design: they are physical artifacts of the Mac and of production, and placeholders would be fabricated evidence."
  },
  "trackA": {
    "branch": "claude/control-centre-full-activation-20260729",
    "engineeringHead": "5e6d4ea37d5f181fbf0ab3a69a8cdb29ad6ca465",
    "executableCandidate": "38d26fdccf7c86414c6d6bc4dfb80ec6520a5aa2",
    "status": "AWAITING_MAC_RAIL_B_VALIDATION",
    "note": "Not merged into this branch. Its scope, candidate, tag and approval marker are untouched."
  },
  "nextExactAction": "On the actual Mac, from a clean dedicated worktree: git checkout 1167b3db30557185b6371df64bad96c67bac3432 && scripts/release/claude/mac-rail-b-hardening-milestone.sh preflight. Then h01, h01-validate (only on a zero-violation report), finalise, marker-command, deploy. Nothing further is possible in the Linux cloud container: no Darwin, no Docker daemon, no ssh binary.",
  "lastUpdatedAt": "2026-07-30",
  "codeHead": "2912482d34187c5aaad02c7c58590ba5dfe69624",
  "stateBasedOnHead": "2912482d34187c5aaad02c7c58590ba5dfe69624",
  "stateCommit": "PENDING",
  "headResolutionRule": "Always resolve the LIVE remote branch head first. Never reset to codeHead: it is the commit this state described, not necessarily the newest commit.",
  "moduleCompletionCriteria": [
    "real caller path works",
    "security boundary works",
    "retry path works",
    "failure path works",
    "recovery path works",
    "UI contract works",
    "production acceptance is defined"
  ]
}
